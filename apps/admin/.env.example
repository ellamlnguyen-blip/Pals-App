# Copy to .env.local when needed. This placeholder runs without credentials.
# APP_ENV is separate from NODE_ENV, and defaults to local.
APP_ENV=local
# Configure integrations in their assigned tasks. Never add privileged keys
# to a NEXT_PUBLIC_ variable. Use local Supabase credentials only for local work.
