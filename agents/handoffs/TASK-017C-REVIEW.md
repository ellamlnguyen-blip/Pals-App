# TASK-017C exact-tip review and coordinator QA

Date: 2026-09-24  
Reviewed implementation task tip: `8f1557bd1ea56eef76904907b9737c8745619e48`  
Local coordinator integration: `e225e3e`

An independent exact-tip source/security review found no remaining P0/P1/P2 issue after two P1 corrections. The first correction clears queue metadata and detail on an authoritative denial. The second binds late mutation responses to the captured session, selection generation and report ID so an earlier report cannot clear or retarget a later selection. A separate initial sign-in role-response race was corrected and live retested. Seven focused flow tests, admin lint, TypeScript and production build passed on the task tip.

The coordinator then exercised the production admin build against disposable local Auth/PostgREST and the accepted default-off moderation backend. Sign-in/sign-out, admin role visibility, neutral nonoperator denial, audited queue and detail, 24-item cursor paging, start/annotate/close/reopen, duplicate linkage, account ban/reinstatement and Hangout disable were observed. A deliberately dropped response after an upstream commit produced the in-session uncertain-outcome state; Retry used the same request key and produced exactly one reopen audit. Gate-off during an open report cleared the queue and detail. Forged-Origin and sensitive response headers were checked by the implementer.

Final coordinator checks used three live local reports. A report targeting a missing account showed `unavailable` without an enrichment reader. Rapid A-to-B selection ended with B's report ID and allegation under B's heading. A second moderator advanced B from revision 0 to 1; the first operator's confirmation still showed revision 0, and its submission received a neutral unavailable state with no stale detail retained. The same operator was separately suspended and banned in the local database; both subsequent queue reloads received that same neutral denial. The dark sign-in and detailed operator view were rendered and inspected for readable contrast and hierarchy. Earlier light-mode browser checks covered approximately 1280, 820, 390 and 320 CSS pixels, with no document overflow, as well as keyboard focus and modal behavior. The macOS appearance preference was restored to its original Light setting.

The final disposable database reset returned Auth users, reports, cases, audit, retry requests, sanctions and Hangout disables to zero; all eight feature gates were false. The local admin server, Supabase stack and Lima runtime were stopped. No hosted migration, deployment, live account sanction or default-on gate occurred. This review accepts the bounded Stage C for local integration. Hosted operations and production retention remain separate decisions.
