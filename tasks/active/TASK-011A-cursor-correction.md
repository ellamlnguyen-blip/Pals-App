# TASK-011A cursor correction — People pagination

Status: Corrective task remains open after exact-commit review found a valid-name cursor gap; revised contract below
Date: 2026-09-22
Parent: TASK-011; decision: Accepted ADR-0013
Starting verified main: `02853e8be789f62056dd8a58fe0fcc4fa3cf4383`
Implementation branch: `agent/TASK-011A-cursor-correction` from verified main `487b744db361559dee8b63c07297ca6bf2d57842`

## Trigger and goal
TASK-011B review found a concrete pagination defect in the reviewed TASK-011A API. The database sorts and compares `lower(btrim(real_name)) COLLATE "C"` but the UI derives `p_after_name` with JavaScript `trim().toLowerCase()`. For an accepted name surrounded by U+00A0 nonbreaking spaces, JS produces `ada` (UTF-8 hex `616461`), while the disposable local database preserves those spaces (`c2a0616461c2a0`). The supplied cursor can skip or repeat rows. Fix the backend cursor contract so the database derives the sort key from the last returned card's account ID, and the UI sends only that ID without guessing PostgreSQL normalization or copying an unbounded raw name. This implements ADR-0013's already accepted bounded deterministic pagination; it introduces no new visibility policy.

## Allowed work
The first additive migration `20260922000500_people_raw_name_cursor.sql` is already integrated but did not fully close the defect: the original profile constraint accepts arbitrarily long raw names if ASCII `btrim` is 1–100, while that migration caps raw `p_after_name` at 100. An exact-commit fresh reviewer reproduced `repeat(' ',100)||'A'` as a valid visible name whose returned raw cursor fails with 22023. Do not narrow owner name data or edit an applied migration. Add a second additive **local-only** migration replacing only `public.browse_people` with the same signature/five fields/grants/gate/readiness/campus/block/filter/limit semantics. A page cursor is `p_after_id` alone; require `p_after_name` to be null. Derive the last-row sort key by a database lookup of that account ID under the *same current viewer authorization*, subject visibility and current search/year/major filters. Hidden, blocked, opted-out, unready, nonexistent and filter-mismatched IDs have one unavailable result shape and disclose no subject data. Then compare `lower(btrim(cursor_subject.real_name)) COLLATE "C"` plus ID against the same sort expression for page rows. Do not add peer output fields or a new subject oracle. Review UUID tie-breaking and post-commit revocation. A concurrently changed subject may invalidate a cursor and require restarting; do not reuse stale authorization. Reject malformed/overlong cursor and preserve literal name search. If an alternate equally narrow database-derived cursor is needed, surface it to the coordinator before changing output fields.

Update actual-role SQL tests with at least 25 opted-in ready same-campus peers, including nonbreaking-space, same-name and over-100-raw-character/short-trimmed boundary cases. Prove two pages enumerate each authorized ID exactly once in database order and cannot leak hidden/blocked/opted-out/filter-mismatched cursor subjects. Include direct authenticated PostgREST RPC pagination using only the last-row ID; reject p_after_name and malformed cursor shapes. Maintain stronger-isolation denial and existing profile/photo/Hangout access. Update only relevant API docs/tests plus `agents/handoffs/TASK-011A-CURSOR.md`; no UI, shared queue, ADR, hosted work or unrelated fixes.

## Verification and sequencing
Continue the same bounded corrective backend agent on its task branch from latest origin/main, after this revised contract is published. The first reviewed implementation did not pass exact-commit independent review and TASK-011A cursor correction remains incomplete. GPT-6 Sol medium with Standard speed as app preference; speed cannot be set or verified through agent dispatch. Run two clean local migration/test resets and lint, real HTTP cursor test, all relevant existing regressions and direct workspace checks; disclose offline tooling limits. Clean fixtures, leave both gates false and stop local services. Commit/push and verify task remote SHA, then stop. Fresh read-only security review of the replacement definer function and authorization is required. Coordinator integrates reviewed fix into main before TASK-011B finalizes its consumer and is integrated.
