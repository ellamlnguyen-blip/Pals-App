# ADR-0010 — Hangout foundation and private-location authorization

Status: Accepted — revised TASK-007 prerequisite decision
Date: 2026-09-22

## Context
Accepted ADR-0004 establishes casual, open-by-default Hangouts without required capacity. Product/data/authorization/location specifications require campus/friends/invite visibility, host/co-host roles, membership and protected exact meeting details but do not define concrete lifecycle transitions, removal/re-entry, co-host powers or exact-location revocation. Existing identity and photo records are owner-only; friendship, invitations, blocks and eligibility data are not implemented. TASK-005 must not infer their permissions or expand peer-profile access.

## Decision
1. Limit this increment to campus-visible, unrestricted Hangouts. Database constraints/operations reject all restricted modes and eligibility payloads; do not merely hide controls. Future modes require accepted policies plus authoritative friendship/invitation/profile data and automated denial tests.
2. Use a Hangout row with UUID ID/university/host references, required nonblank title and start timestamp, optional description/end timestamp, published/cancelled lifecycle, open/closed joining (default open), campus visibility and server-owned created/updated timestamps. End, if present, must follow start. Immutable host and campus are derived from the live caller, not trusted client inputs. No stored draft, attendance/completed inference, host transfer or physical client deletion. Cancellation closes joining and is terminal for this increment. Concrete accepted bounds and write semantics are specified below.
3. Store public place label, public latitude/longitude, optional campus zone and explicit precision separately from optional private exact details. Validate coordinate ranges. Public coordinates must be deliberately provided as a safe approximate meeting area; do not copy exact coordinates into public fields or derive a predictable offset that promises privacy. Store private meeting instructions separately, not hidden columns in an otherwise publicly selectable row. No user/device location storage.
4. Use one participant row per Hangout/account, with joined/left/removed state and server-owned transition timestamps. Host invariant: creator is atomically a joined participant and cannot leave or be removed while host; cancel instead. A participant may leave; a left participant may rejoin a published/open Hangout after all live access checks. Removed participants cannot self-rejoin. Removal history must not be erased by an upsert. Attendance confirmation is deferred.
5. Minimal role boundary: immutable host ownership authorizes edit/cancel/open-close/private-detail management and nonhost removal. Reserve co-host design for TASK-010; reject client co-host assignment and grant no co-host privileges now. Platform admin/moderator membership gives no automatic Hangout/private-location bypass. Audited moderation is a later task.
6. Require live ready access and matching campus for every client operation. Published campus Hangouts and current joined participant account IDs may be read by those campus readers, consistent with accepted public attendance; no peer profile/photo or historical membership disclosure. Cancelled-record access: host and still-joined participants only. Host and still-joined participants may read private details only while published and live-ready; leaving/removal/cancellation/readiness loss revokes future reads immediately. Previously received information cannot be recalled. No time-based expiry is inferred while the Hangout remains published; this retention choice was explicitly accepted as recorded below.
7. Use least-privilege grants/RLS and transactional database operations for identity-bound state transitions. Prevent generic row updates from bypassing invariants; test public API paths and stale callers. Closed joining blocks new joins but does not evict current participants. Serialize conflicting join/remove/cancel transitions so a losing operation cannot restore revoked access.

## Accepted concrete resolution — TASK-007 prerequisite review

This revision resolves the earlier open questions into a local-development policy explicitly accepted by the user on 2026-09-22. The acceptance evidence below records reconciliation with the concurrently published earlier version.

1. **Field/time bounds:** trimmed plain-text title 1–120 characters; nullable description 1–2000; public place label 1–120; nullable campus zone 1–80; nullable private meeting instructions 1–2000. Blank optional strings normalize to null; the database rejects non-normalized direct input. Latitude/longitude must be finite and within geographic bounds. Public precision is `approximate_area` only in this increment, selected deliberately by the host. No private coordinate fields or automatic exact-to-public offset. On creation or a changed start, start must be between the database current time and 366 days ahead; end, if supplied, must be after start and at most seven days later. An unchanged past start does not prevent other edits/cancellation. TASK-005 stores/validates instants as timestamptz; later TASK-007 web input/display identifies America/New_York and rejects nonexistent or ambiguous local times unless explicitly disambiguated.
2. **Lifecycle/retention:** published → cancelled is terminal; cancellation atomically closes joining and keeps membership/history for authorization and future safety work. Cancelled records reject public/private detail edits and reopening joining at the database boundary. No client hard delete, restore, completed state, stored drafts or removal reversal. Cancelled public records are readable only by the live-ready same-campus host and still-joined participants. Private details become unreadable to all client roles on cancellation, including the host. Stored records are retained in this local increment; this is not a production data-retention policy.
3. **Host/roster:** creator is atomically joined and cannot leave/be removed. Only the host edits/cancels/opens/closes/removes; no co-host powers yet. A left participant may rejoin while published/open; a removed participant cannot. Passing start/end alone does not close joining; the host must close or cancel. Published current-participant IDs are visible to live-ready same-campus readers only if that participant also currently meets ready access in that campus. No historical membership disclosure to campus readers, peer profile/photo expansion, or moderator bypass. Host may inspect nonpublic membership states needed for removal; a participant may inspect their own state. Removal revokes private access, not ordinary campus-public visibility. Safety reporting after removal remains a later launch prerequisite.
4. **Private access:** host and joined participants only, while published and currently live-ready in the same campus. Leaving/removal/cancellation/readiness loss revokes future reads; closed joining does not revoke current membership. There is deliberately no automatic end-time expiry in this accepted increment: access continues until one of those revocations. Previously read information cannot be recalled. The UI explains this. Require a deliberate public-area selection and clear warning that public label/pin/description must not contain room numbers or sensitive meeting instructions; exact instructions have a separate optional field and protected storage. Validation cannot prove user-supplied public text is safe.
5. **Safety sequencing:** permit TASK-005 backend and later TASK-007 UI verification only in disposable local development before blocking is implemented. Add a database-controlled Hangout feature gate, disabled by default and not client-writable, required on every exposed Hangout read/write/RPC. Enable it only in explicit local test setup; test disabled-gate denial. TASK-007 additionally denies its pages/actions outside local configuration and the validated loopback Supabase target. This is defense in depth, not a replacement for RLS. No hosted migration, live student flow, hosted enablement or deployment is authorized. Block/private-access precedence must be accepted, implemented and tested in a separate bounded prerequisite before hosted integration; full reporting/moderation/launch safeguards remain required. This deliberately defers that policy rather than inventing it inside TASK-005/007.
6. **Atomic create/edit boundary:** TASK-005 supplies caller-bound transactions that create public record, private instructions and host membership together, and update public/private details together. Failed transactions leave no partial Hangout or public/private mismatch. A server-owned monotonically increasing revision and expected-revision checks reject stale detail/lifecycle writes. An owner-scoped creation request UUID deduplicates retries; same UUID and same normalized creation payload returns the original identifier after current authorization checks, while conflicting reuse is rejected. A valid retry is resolved before new-creation time-window validation, so a response lost near the start time remains recoverable. Never return original private payload on replay after revocation. Test lost-response retry and concurrent calls. These primitives belong to TASK-005; TASK-007 consumes them and must not add an alternate persistence path.

## Acceptance evidence and concurrent revision reconciliation
On 2026-09-22 the TASK-005 task recorded the user's approval to complete TASK-005 and published the earlier ADR at main `895923b` (acceptance commit `0355b3c`). That version retained all joined roster IDs, wider description/place/zone limits and unrestricted finite dates.

The user subsequently answered **“1. yes / 2. yes”** in TASK-007 to the explicit requests (1) to publish the reviewed planning documents to the public canonical Pals-App repository and integrate main and (2) to accept the revised ADR linked from commit `f049184837cfe4279b824d19bf09f9239e3b79ad`. The linked revision is the decision above. It supersedes the earlier roster/field/time choices: current-ready participant IDs, the stated narrower text limits and future create/changed-start bounds. It also explicitly requires the default-disabled database feature gate and atomic retry/revision semantics. Earlier approval remains historical evidence, not a competing implementation policy.

TASK-005 remains a separate backend implementation owned by its existing task/agent; no duplicate dispatch. Its coordinator was notified to reconcile against this latest accepted revision before integration. Review, test, integrate and remote-verify TASK-005 before TASK-007 dispatch. Acceptance does not establish backend completion. Hosted changes, live student flows and deployment remain unauthorized.

## Consequences
Separating private details reduces accidental disclosure through public reads/embeds. A narrow campus-only implementation can be tested without speculative social rules. Requiring readiness in database policies preserves immediate revocation independently of page guards. Deferred restricted modes/co-host/block/moderation behavior require later reviewed contracts; this backend foundation is not launch-ready by itself. Local committed migrations are authorized only under the separate TASK-005 contract. Hosted changes remain excluded.

## Alternatives
- Implement all visibility/eligibility modes now: rejected for this increment because authoritative supporting data/policies are absent and the scope would grow materially.
- Store exact details on the public row and omit them in the UI: rejected; client query shape cannot provide database confidentiality.
- Implement block/co-host/moderation subsystems inside TASK-005: defer to explicit bounded prerequisite tasks if acceptance requires them.
- Delay the entire Hangout foundation until all social/safety features exist: not selected; local foundation may proceed behind the accepted gates.
