# TASK-021 staging readiness and rehearsal plan
Date: 2026-09-25
Evidence baseline: original audit at `737ca32f1eefd4b347940e367b3923b47c2e0884`; TASK-010 repository status refreshed from independently verified canonical main `ac172980d4516ce54d3804c30393b225c0f5ea2c`. Hosted state remains uninspected.
Disposition: **not ready for hosted rehearsal or launch**. This is a repository audit, not a current hosted inspection or a new runtime test result.

## Readiness matrix
All source paths below are repository-relative. Historical task passes establish only their documented local scope.

| Dependency | Evidence / present boundary | Required closure and owner |
| --- | --- | --- |
| Host/co-host management | `agents/handoffs/TASK-010.md`, ADR-0012: backend and UI reviewed and integrated for disposable-local scope; no hosted operation. Final-tip browser Step down click/screenshots were unavailable, with SQL/HTTP coverage recorded. | Release owner reviews the exact co-host migration and app build against the selected hosted target, current authorization/gates and remaining UI evidence. Local completion does not authorize hosted release. |
| Deployed identity | TASK-003 contract leaves combined callback criterion open. `HOSTED_ENVIRONMENT.md` records only historical foundation/onboarding migrations, localhost site URL/callbacks and no configured custom SMTP. | Deployment owner selects exact authorized target and HTTPS origin; email owner configures approved SMTP; approved controlled real UNC test mailbox (not a wholly synthetic identity) exercises delivery/confirmation/resend/sign-in/out and restriction gates. Reinventory first; historical zero users is not current proof. |
| Hosted migration and app release | DEPLOYMENT and local ADRs restrict later feature use. Existing hosted authorization covers recorded foundation scope, not the complete local migration chain. Static baseline checks confirm local-only guards in `apps/admin/lib/config.ts` and `apps/web/lib/hangouts.ts`, `people.ts`, `notifications.ts`, `safety.ts`, and `analytics-core.ts`. | Release engineer inventories current migration history/configuration, reviews exact pending manifest and app guard changes, prepares backups/recovery and bounded target-specific authorization. Never strip local-only checks as a rehearsal shortcut. |
| Moderation operations/retention | ADR-0019 and TASK-017 handoffs: local report queue and sanctions; hosted staffing, retention/deletion/legal hold, appeals/escalation and MFA undecided. | Product/safety owner accepts operational policy; security reviewer verifies least privilege, operator conflicts/bootstrap/revocation, auditable actions and retention/recovery. Name owners and verify deletion/export/retention handling separately for reports, cases, immutable audits, retained chat/DM/attendance and the private size signal; no blanket cleanup cascade. Named operators and response coverage are required; no presumed staffed service. |
| Preissued photo URLs | ADR-0021 accepts only local new-request revocation; previously issued bearer URLs survive to expiry; hosted cache behavior excluded. | Product/security owners choose hosted lifetime/sharing/incident response and any required revocation mechanism; verify actual hosted behavior and copy. Do not promise immediate revocation of delivered material. |
| Large-Hangout signal | ADR-0024, TASK-020 handoff: 25 joined rows, host coarse warning, viewer-safe map dampening and private unconsumed observation. | Separate accepted consumer, audited access, retention/response policy and tested implementation before hosted use. The reviewed proposed options and missing operating choices are in `TASK-021-SIZE-SIGNAL-DECISION.md`. ADR-0019 remains submitted-report-only; a size observation is no allegation or sanction. |
| Restricted visibility and eligibility | MVP includes campus/friends/invite-only and eligibility; BACKLOG says restricted modes remain disabled. | Product owner resolves full MVP implementation versus explicit accepted scope change. Invitation/eligibility access needs bounded policy/contracts and RLS tests; friendship completion alone grants no Hangout access. |
| Friend context and ranking | MVP requires friend-attendance context and friend-aware ranking; TASK-009/012 and BACKLOG defer these. | Separate bounded implementation/accepted scope decision; no popularity scoring or hidden-member ranking input. |
| Peer profile/photo scope | MVP rich profiles versus TASK-011's opt-in limited text with peer photos deferred. | Explicit launch-scope reconciliation; any photo reader requires an accepted access/delivery policy, not reuse of owner-only Storage permissions. |
| Analytics and outcome coverage | ADR-0023/TASK-019: local sink, nine wired names, five deliberately unwired, private attendance/safety excluded. No repeat-outcome aggregation endpoint. | Decide launch measurement coverage; hosted region/access/deletion/transport/consent review and verifiable raw retention <=90 days before capture. Keep capture off if unmet. Authoritative outcomes stay in Postgres; no raw attendance export. |
| Messaging/notifications | TASK-013/014/015 are local tested increments; hosted delivery/Realtime separate. | Verify accepted manual-refresh behavior and intended launch requirements; any hosted transport change needs its own contract. Test access loss and neutral notification destinations. Realtime is not presumed implemented or independently mandatory for this rehearsal. |
| CI and verification limits | BACKLOG records pre-existing dev request-context/action-manifest failures, original-checkout timeouts and an unresolved SQL-fixture flake. | Engineering owner reruns exact release-tip automation and actual built-server E2E; fix release-blocking harness defects in bounded work or document precise evidence limitations. No inference of green current CI from old local passes. |
| Release operations and supply | RELEASE_CHECKLIST requires backup/rollback, monitoring and genuine initial UNC Hangouts with moderators. | Release/product owners document recovery, incident coverage, promotion and real-student supply plan. Synthetic fixtures must never be presented as real inventory. Live seeding/promotion needs separate authorization. |
| Domain cutover | DEPLOYMENT reserves DNS for later cutover; TASK-022 is separate. | Rehearse on explicitly approved temporary HTTPS origin. Leave purchased-domain DNS untouched. |

Owners above are required responsibilities, not claims that people have been assigned or approved policies.

## Ordered rehearsal and evidence requirements
Every row below is **planned/unrun in TASK-021**. A later contract must name exact build SHA, target project/origins, testers, time window, allowed operations, gate list and cleanup plan. Its acceptance cannot be inferred from this document.

| Phase | Cases and expected evidence |
| --- | --- |
| Preflight | Verify nonproduction identity of every service, exact migrations/config diff, credential scoping, callback allowlist, SMTP recipient authorization, TLS, approved controlled real UNC mailbox/sender and cleanup for resulting Auth/profile/photo records (no public invitations), Mapbox origin restrictions, baseline gates/users/storage and backup/recovery evidence. No secrets in reports. Stop on target mismatch, unexplained migration drift or missing authorization. |
| Identity/profile | Actual UNC inbox delivery through deployed HTTPS confirmation/callback/onboarding/ready; resend, expired/replayed callback, rejected redirects, sign-in/out, incomplete profile, changed email, suspended account, photo owner/nonowner access. Sanitized screenshots and API assertions; no inbox contents/tokens retained. |
| Core loop | Separate approved test accounts create approximate public Hangout and private details; map/Calendar discovery, join, coordinate in chat, leave/rejoin, friendship/DM consent and notifications. Verify persisted outcomes after reload and lost-response retries. Test co-host powers from the reviewed TASK-010 implementation after the exact build and hosted scope are approved; include cancel/removal and stale revisions. |
| Privacy/safety | Anonymous/cross-campus/unready/blocked/removed/disabled callers and direct REST/legacy RPC/Storage paths; exact location never escapes source permission. Block teardown and non-restoration on unblock; retained-ID reporting without source leakage; current operator role, conflict checks, audit, sanction/revocation and source masking. Cover preissued photo URLs separately from new-request denial. |
| Large groups | Threshold 24/25, voluntary close/reopen, no automatic removal/cap; private signal idempotence and authorized consumer once implemented; >100-result ordering and hidden-member invariance; gate-off behavior. No claim of physical attendance or guaranteed host response. |
| Attendance/analytics | Owner-only answer/window/revision and moderation denial; no peer or source recovery via attendance. Consent-off/revoke/sign-out produces no capture; full authorized payload/retention checks if hosted analytics separately approved. Distinguish authoritative commit from event replay and record deliberately missing measurement. |
| UI/reliability | Desktop/phone and keyboard/focus, loading/empty/error/denied states, network failure/stale response/retry recovery, no private data in URLs/logs/analytics/caches. Exercise deployed built app, not just local dev. Preserve known limitation labels. |
| Recovery/cleanup | Stop on any permission leak or unexpected target/traffic. Use only preapproved rollback or forward recovery; never drop hosted data or treat gate-off as undoing migrations/evidence. Remove only tagged authorized fixtures under accepted retention rules; retain required audit evidence. Verify final gate values, accounts/objects, configuration and owned services against approved intended state. |
| Review | Independent security and release evidence review, named unresolved issues and explicit go/no-go. TASK-021 stays incomplete for failed/unrun required checks. No production or DNS authorization follows from a passing rehearsal. |

## Decisions to prepare, in dependency order
1. TASK-010 is complete for disposable-local scope. Review its migration, app build and documented verification limits in the exact hosted release package; no policy reacceptance is needed unless authority changes.
2. Assign product/safety owners and prepare concrete hosted moderation/retention/photo-URL and size-signal consumer proposals for explicit acceptance. Do not invent staffing, retention periods or response commitments in a runbook.
3. Reconcile remaining MVP access, friend context, profile and measurement scope explicitly. Deferred local tasks cannot silently redefine MVP.
4. Prepare the exact staging release package: nonproduction project, origin, migrations, guard changes, providers, test identities, temporary gates, backup/recovery and cleanup. Only then seek hosted execution authorization.

Planning may proceed independently. Hosted execution cannot start until its policy and target-specific authorization dependencies are satisfied. No completion-triggered successor is created while TASK-021 remains open.
