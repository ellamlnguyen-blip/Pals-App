# TASK-006 profile interaction plan

Reading this as a private account editor for UNC students, preserving the friendly campus-blue and white Pals language. Leon Taste read; live usepals.com inspected in the in-app browser on 2026-09-22 (rounded blue hero, chunky white type, pale surfaces). Its navigation and event wording are not authority. Existing Nunito, shared blue/white tokens and native controls remain the foundation. Variance 3, motion 2, density 4.

Header avatar opens /profile. A prominent Back to Hangouts link precedes the owner-only notice. Desktop shows a narrow identity/photo column and wider editable details; phone stacks these without horizontal overflow. University/email are read-only; verification says UNC email verified. Required fields stay first; optional sections clearly say optional and may remain empty. Three fixed prompt pairs and one-item-per-line interest lists keep bounds legible.

Text editing has explicit Edit, Save and Cancel states. Controlled inputs preserve entered values across errors. Save pending disables conflicting controls and announces progress. Stale saves explain the conflict and require reloading current values rather than silently replacing newer edits. Photos save individually; uploads occur before replacement, remove detaches before cleanup. Each operation carries a revision; failures leave existing references intact. Cleanup failure explains that private unused uploads remain, with a retry control using server-selected owned unreferenced objects. No browser-provided object path determines a stream or mutation.

Photo cards show the primary plus insertion-ordered extras, with add/replace/remove controls and readable missing-image fallback. Limit four extras, no reordering or promotion. Photos remain owner-only; current image metadata is preserved. Photos and text share revision state to avoid stale references. Loading route, inline errors, save success, empty optional/photo slots, keyboard focus and desktop/phone layouts will be inspected against the local disposable account workflow. No profile enrichment gates onboarding.
