# Web and Mobile Strategy
Build the complete usable product on web first for faster iteration/testing.

Mobile second: React Native + Expo for iOS/Android.

Reuse Supabase backend, domain rules, types, validation, compatible data access, and design tokens. Do not force identical UI/navigation/component implementations.
